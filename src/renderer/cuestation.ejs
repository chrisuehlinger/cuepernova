<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black">
  <title>Cuepernova Cuestation</title>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      height: 100vh;
      width: 100vw;
    }

    body {
      background: black;
      overflow: hidden;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    }

    #its-showtime {
      position: absolute;
      top: 0;
      left: 0;
      display: none;
    }

    /* Fullscreen content helpers */
    .fullscreen-frame {
      width: 100%;
      height: 100%;
      border: none;
    }

    .fullwidth {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }

    .full-area {
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      background: black;
    }

    /* Debug screen */
    .debug-wrapper {
      width: 100%;
      height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background: #333;
      color: white;
      font-family: monospace;
      border: 10px dashed dodgerblue;
    }

    .debug-wrapper h1 {
      font-size: 3em;
      margin: 20px;
      text-align: center;
    }

    .debug-wrapper strong {
      color: #00ff00;
    }

    /* Freeze screen */
    .freeze-wrapper {
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      background: white;
    }

    .freeze-wrapper h1 {
      font-size: 10em;
      color: red;
      font-weight: bold;
      animation: blink 0.5s infinite;
    }

    @keyframes blink {
      0%, 49% { opacity: 1; }
      50%, 100% { opacity: 0; }
    }

    /* Generic message screen */
    .message-wrapper {
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      background: black;
      color: white;
      text-align: center;
      padding: 50px;
    }

    .message-wrapper h1 {
      font-size: 96px;
    }

    .message-wrapper p {
      font-size: 2em;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div id="its-showtime"></div>
</body>
</html>